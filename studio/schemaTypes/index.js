import { localeString, localeText, localeStringList, localeBody } from './localized';
import { project } from './project';
import { blogPost } from './blogPost';

export const schemaTypes = [localeString, localeText, localeStringList, localeBody, project, blogPost];
